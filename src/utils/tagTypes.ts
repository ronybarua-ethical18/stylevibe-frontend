export enum tagTypes {
  SERVICES = 'services',
  USER = 'user',
  BOOKINGS = 'bookings',
  TRANSACTIONS = 'transactions',
}

export const tagTypesList = [
  tagTypes.SERVICES,
  tagTypes.BOOKINGS,
  tagTypes.TRANSACTIONS,
]
